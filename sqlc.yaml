version: "2"
sql:
  - engine: "sqlite"
    queries: "queries"
    schema: "schema.sql"
    gen:
      go:
        emit_pointers_for_null_types: true
        emit_empty_slices: true
        emit_json_tags: true
        package: "db"
        out: "db"
